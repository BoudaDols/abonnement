apiVersion: v1
kind: ConfigMap
metadata:
  name: abonnement-config
data:
  APP_ENV: "prod"
  APP_DEBUG: "false"
  DB_CONNECTION: "mysql"
  DB_HOST: "mysql"  # Kubernetes MySQL service name
  DB_PORT: "3306"
  DB_DATABASE: "abonnement"
  PAYMENT_GATEWAY: "stripe"
